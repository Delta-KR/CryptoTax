node_modules
/.next
/out
/build
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# Email build output — regenerated via `npm run email:build`
/emails/dist

# Secrets / personal — never commit
recovery-codes.txt

# graphify knowledge graph — regenerable via `graphify update` or git post-commit hook
graphify-out/
